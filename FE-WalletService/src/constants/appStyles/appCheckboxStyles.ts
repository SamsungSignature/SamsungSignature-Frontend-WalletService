import {AppCheckboxStyle} from '../../components/common/AppCheckbox';

const SMALL: AppCheckboxStyle = {
  size: 20,
};

interface CheckboxStyles {
  SMALL: AppCheckboxStyle;
}

const CHECKBOX: CheckboxStyles = {
  SMALL,
};

export default CHECKBOX;
